doctype html
html(lang="en")
  head
    title Population Information
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="styles.css")
  body
    header
      h1 Population Information
    nav
      ul
        li
          a(href="#") Population Information
        li
          a(href="/city") City
        li
          a(href="/about") About
    main
      h2 Welcome to Population Information
      form(action="/submit", method="post")
        label(for="data") Data:
        textarea(id="data", name="data")
        button(type="submit") Submit
        // add a script to handle form submission and database interaction
    script.
      const form = document.querySelector('form');
      form.addEventListener('submit', (event) => {
        event.preventDefault(); 
        const data = new FormData(form); 
        fetch('/submit-form', {
          method: 'POST',
          body: data
        }).then(response => {
          
        });
      });
    footer
      p Copyright © Population Information 2023
